1. Finish models/index.js
    - Need to complete:
        - Products belongToMany Tags (through ProductTag)
        - Tags belongToMany Products (through ProductTag)

COMPLETE ^^^^^^^^^**************************************

2. Finish routes/api/category-routes.js
    - Need to complete:
        - router.get('/:id')
        - router.post('/')
        - router.put('/:id')
        - router.delete('/:id')

3. Finish routes/api/products-routes.js
    - Need to complete:
        - router.get('./:id')
        - router.post('/')
        - router.delete('/:id')

4. Finish routes/api/tag-routes.js
    - Need to complete:
        - router.get('/:id')
        - router.post('/')
        - router.put('/:id')
        - router.delete('/:id'
